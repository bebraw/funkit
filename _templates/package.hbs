// GENERATED! DO NOT MODIFY!
define(['require', 'funkit/functional/chain', 'funkit/functional/each', 'funkit/object/values', {{#list modules}}'./{{package}}/{{name}}'{{/list}}],
        function(require, chain, each, values) {
    var functions = {
        {{#list modules}}'{{name}}': require('./{{package}}/{{name}}'){{/list}}
    };
    var ret = chain(values(functions));

    each(function(k, v) {
        ret[k] = v;
    }, functions);

    return ret;
});
