var suite = require('suite.js');
var f = require('funkit/math');

/*
XXX: enable once suite.js can generate tests more effectively
suite(f.acos, function(op, a) {
    return f.between(op(a), 0, Math.PI);
});
*/
